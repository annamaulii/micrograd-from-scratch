[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "micrograd-from-scratch"
version = "0.1.0"
description = "A scalar autograd engine and multilayer perceptron implemented from scratch."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
include = ["micrograd*"]